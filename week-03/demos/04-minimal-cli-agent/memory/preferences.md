# User preferences

- The user's working documents live in `workspace/` (relative to the agent's folder).
- The user likes answers as short bullet lists — no long paragraphs.
- Dates in ISO format (YYYY-MM-DD).
