[project]
name = "single-file-rag"
version = "0.1.0"
description = "Single-file RAG — cheating at solitaire: paste the whole file into the prompt, watch prompt_tokens"
license = "CC-BY-NC-SA-4.0"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]
